{
  "name": "@kf/game-utils",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src --root-mode upward --out-dir dist --delete-dir-on-start --no-comments"
  },
  "dependencies": {
    "namespace-constants": "^0.5.0",
    "redux-actions": "^2.6.4"
  },
  "devDependencies": {
    "jest-canvas-mock": "^2.1.0"
  }
}
