{
  "name": "@kf/game-engine",
  "version": "0.0.0",
  "main": "./es/index.js",
  "license": "MIT",
  "dependencies": {
    "@kf/game-utils": "^0.0.0",
    "flat": "^4.1.0",
    "pixi.js": "4",
    "ramda": "^0.26.1",
    "rxjs": "^6.5.2",
    "scale-to-window-pixi": "^1.2.0"
  },
  "scripts": {
    "build": "babel src --root-mode upward --delete-dir-on-start --no-comments",
    "watch": "babel src --root-mode upward --delete-dir-on-start --no-comments -w --verbose"
  },
  "devDependencies": {
    "jest-canvas-mock": "^2.1.0"
  }
}
